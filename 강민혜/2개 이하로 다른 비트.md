# 2개 이하로 다른 비트

## 풀이

```js
function solution(numbers) {
  const answer = [];
  numbers.forEach((number) => {
    if (number % 2 === 0) {
      answer.push(number + 1);
    } else {
      number = "0" + number.toString(2);
      for (let i = number.length - 1; i >= 0; i--) {
        if (Number(number[i]) === 0) {
          answer.push(
            parseInt(
              number.substring(0, i) +
                "10" +
                item.substring(i + 2, item.length),
              2
            )
          );
          break;
        }
      }
    }
  });
  return answer;
}
```
